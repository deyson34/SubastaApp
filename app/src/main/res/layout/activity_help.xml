<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Contenedor de preguntas y respuestas -->
        <LinearLayout
            android:id="@+id/faq_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"/>

        <!-- EditText para ingresar un problema -->
        <EditText
            android:id="@+id/problema_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Escribe el problema"
            android:padding="8dp"
            android:layout_marginTop="24dp"
            android:background="@drawable/edit_text_background"
            android:minHeight="100dp"
            android:gravity="top"/>

        <!-- Botón para enviar el problema -->
        <Button
            android:id="@+id/enviar_problema_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enviar Problema"
            android:layout_marginTop="12dp"
            android:paddingHorizontal="16dp"
            android:backgroundTint="@android:color/holo_blue_light"/>
    </LinearLayout>
</ScrollView>
